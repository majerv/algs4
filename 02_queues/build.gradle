apply plugin: 'java'
apply plugin: 'eclipse'

repositories {
    mavenCentral()
}

dependencies {
	compile files('../common_lib/algs4.jar', '../common_lib/stdlib.jar')
	
    testCompile 'junit:junit:4.12'
    testCompile 'com.google.truth:truth:0.25'
}

sourceSets {
    main {
        java {
            srcDirs = ["src"]
        }
    }
    test {
        java {
            srcDirs = ["test"]
        }
    }
}